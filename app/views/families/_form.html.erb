

<%= form_with(model: family, local: true) do |form| %>
  <% if family.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(family.errors.count, "error") %> prohibited this family from being saved:</h2>

      <ul>
      <% family.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_area :name, id: :family_name ,:required =>true %>
  </div>

  <div class="field">
    <%= form.label :address %>
    <%= form.text_area :address, id: :family_address,:required =>true  %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
